services:
  - type: web
    name: backend
    env: node
    rootDir: backend/
    buildCommand: npm install
    startCommand: node server.js
  - type: web
    name: frontend
    env: static
    rootDir: frontend/
    buildCommand: npm install && npm run build
    publishDir: dist/
